<UserControl x:Class="ExamFinalePt1NR.Views.UsersControls.Livre.FindLivreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExamFinalePt1NR.Views.UsersControls.Livre"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="750" Background="White">
    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Titre -->
            <TextBlock Grid.Row="0"
           Text="Rechercher un Livre"
           FontSize="22"
           FontWeight="Bold"
           HorizontalAlignment="Center"
           Margin="0,10,0,10"/>

            <!-- Critère -->
            <Grid Grid.Row="2" Margin="10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Grid.Column="0"
                               Text="Mot clé :"
                               FontSize="14"
                               FontWeight="Bold"
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    
                    <TextBox x:Name="TxtCritere"
                             Grid.Column="1"
                             Height="28"
                             FontSize="14"
                             Width="300"
                             Margin="10,0"/>

                    <Button Grid.Column="2"
                            Background="#297fb8"
                            Content="Rechercher"
                            Foreground="White"
                            FontSize="14"
                            FontWeight="Bold"
                            Padding="5"
                            Margin="5"
                            Width="100"
                            Click="BtnRechercher_Click"/>

                    <Button Grid.Column="3"
                            Background="#7e8b8d"
                            Content="Tout"
                            Foreground="White"
                            FontSize="14"
                            FontWeight="Bold"
                            Padding="5"
                            Margin="5"
                            Width="80"
                            Click="BtnAfficherTout_Click"/>
                </StackPanel>
            </Grid>

            <!-- Tableau -->
            <DataGrid x:Name="DgLivre"
                      Grid.Row="4"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Titre" Binding="{Binding Titre}" Width="200"/>
                    <DataGridTextColumn Header="Auteur" Binding="{Binding Auteur}" Width="150"/>
                    <DataGridTextColumn Header="Catégorie" Binding="{Binding Categorie}" Width="150"/>
                    <DataGridTextColumn Header="Quantite" Binding="{Binding Quantite}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            
        </Grid>
    </Grid>
</UserControl>
