<UserControl x:Class="ExamFinalePt1NR.Views.UsersControls.Livre.FindLivreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExamFinalePt1NR.Views.UsersControls.Livre"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Titre -->
            <TextBlock Grid.Row="0"
           Text="Rechercher un Livre"
           FontSize="22"
           FontWeight="Bold"
           HorizontalAlignment="Center"
           Margin="0,10,0,10"/>

            <!-- Critère -->
            <Grid Grid.Row="2" Margin="20,0,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
               Text="Critère :"
               FontSize="14"
               VerticalAlignment="Center"/>

                <TextBox x:Name="TxtCritere"
 Grid.Column="1"
 Height="28"
 FontSize="14"
 TextChanged="TxtCritere_TextChanged" Grid.ColumnSpan="2" Margin="15,5,0,-5"/>
            </Grid>

            <!-- Tableau -->
            <DataGrid x:Name="DgLivre"
          Grid.Row="4"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="True"
          HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Titre" Binding="{Binding Titre}" Width="120"/>
                    <DataGridTextColumn Header="Auteur ($)" Binding="{Binding Auteur}" Width="90"/>
                    <DataGridTextColumn Header="Categorie" Binding="{Binding Categorie}" Width="90"/>
                    <DataGridTextColumn Header="Quantite" Binding="{Binding Quantite}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>

            
        </Grid>
    </Grid>
</UserControl>
